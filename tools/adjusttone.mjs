export function run(input) { const text = (input && typeof input.text === 'string') ? input.text.trim() : ''; const style = (input && typeof input.style === 'string') ? input.style.trim().toLowerCase() : ''; if (text === '') return ''; if (style === 'kind') return 'Thank you! ' + text + ' Have a nice day!'; if (style === 'funny') return text + ' 😄'; return text; }
